name: my_project

packages:
  - apps/**
  - packages/**

command:
  version:
    branch: main
    releaseUrl: true

scripts:
  format:
    run: melos exec dart format . --fix
    description: Run 'dart format' for all packages